apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: {{ .Values.postgres.pvc.claimName }}

spec:
  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: {{ .Values.postgres.storageSize }}